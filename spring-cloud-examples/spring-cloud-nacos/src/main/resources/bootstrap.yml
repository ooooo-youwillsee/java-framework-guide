server:
  port: 0

spring:
  application:
    name: demo-spring-cloud-nacos

  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: centos1:8848
      config:
        namespace: test
        file-extension: yml
        group: COMMON_GROUP
      discovery:
        namespace: test
        group: COMMON_GROUP