server:
  port: 0

spring:
  application:
    name: demo-spring-cloud-consul

  cloud:
    consul:
      config:
        prefix: config
        format: yaml
        # 默认加载这个配置文件
        default-context: application
        enabled: true
      host: centos1
      port: 8500


